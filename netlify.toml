[build]
  command = "npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "20"

# SPA fallback so deep links like /group/:id resolve to the React app.
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
